


<h1>User Index</h1>
<%= form_tag(users_path, method: :get) do %>
  <%= label_tag :search, 'Search by First Name:' %>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag 'Search' %>
<% end %> 
    <% @users.each do |user| %>
    
       <%= link_to "#{user.name}", user %>-
       <%= link_to 'Send Friend Request', new_friend_request_path(recipient_id: user.id) %><br>

     
    <% end %>
 

<!-- Display posts of the current user
<%= link_to "Posts Index", posts_path %>
<h1>All Users</h1>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.name %>
      <% unless current_user == user %>
        <%= link_to 'Send Friend Request', new_friend_request_path(recipient_id: user.id) %>
      <% end %>
    </li>
  <% end %>
</ul> -->



<style>
body {
  background-image: url('/assets/fb.webp');
  background-repeat: no-repeat;
  background-attachment: fixed; /* Keep the background fixed while scrolling */
  background-position: center center;
  width: 100%;
  height: 100vh;
  margin: 0;
}
</style>
